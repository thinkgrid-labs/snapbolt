[package]
name = "snapbolt-cli"
version = "0.2.1"
edition = "2021"
description = "Native high-speed image processing CLI for Snapbolt"
repository = "https://github.com/thinkgrid-labs/snapbolt"

[lib]
 crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16", features = ["async"] }
napi-derive = "2.16"
walkdir = "2.3"
rayon = "1.7"
tokio = { version = "1.28", features = ["full"] }
snapbolt-core = { path = "../core", features = ["native"] }

[build-dependencies]
napi-build = "2.1"

[dev-dependencies]
tempfile = "3"
